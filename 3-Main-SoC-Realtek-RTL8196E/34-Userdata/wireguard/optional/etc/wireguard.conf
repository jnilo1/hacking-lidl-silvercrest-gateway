# WireGuard is shipped but disabled by default.
# Set ENABLED=1 only after creating /userdata/etc/wireguard/wg0.conf.
ENABLED=0
INTERFACE=wg0
ADDRESS=
MTU=1420
# Space-separated destination prefixes routed through the tunnel.  Empty by default.
ROUTES=
