# Keep firmware for users who just want to flash. Only the .gbl is shipped:
# the .s37 is a side-effect of the build (the SWD/J-Link image), and stays
# ignored by the root rule like every other .s37 in the tree.
!firmware/
!firmware/*.gbl
